# Resend credentials for the /api/contact endpoint.
# These are consumed at runtime by the Vercel function; set the real values
# in Vercel → Project → Settings → Environment Variables.

RESEND_API_KEY=
CONTACT_TO=alice@authenticcommunicatorglobal.com
CONTACT_FROM=Authentic Communicator <contact@authenticcommunicatorglobal.com>
